import React from "react"
import { ContentBar } from "./UI/ContentBar.jsx"

export const LastTasks = (props) => {
    return (<ContentBar className="contentbar__last-tasks">
        last tasks
    </ContentBar>)
}